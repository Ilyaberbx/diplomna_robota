# Grill Report — thesis/diploma.pdf — 2026-05-19

## Підсумок
Прохід — цільовий аудит §5 (таблиці), §11 (список джерел) і §1.1 за
зразком захищеної роботи (`docs/diploma-rules/references`). Виявлені
користувачем порушення усунено в `build_pdf.py`, PDF перезібрано (50 с.)
і перевірено візуальним рендером с. 37–38, 50.
Порушень після фіксів: **0 BLOCKER, 0 MAJOR, 0 нових MINOR**.
Вердикт: **PASS WITH FIXES**.

## Усунені порушення

### [ВИПРАВЛЕНО] [MAJOR] §11 + §1.1 — завеликий лівий відступ у списку джерел
- Було: список використаних джерел рендерився як `<ul class="lst">` із
  `padding-left: 12.5mm` на весь блок — кожен перенесений рядок зсунутий
  на 12,5 мм (с. 50, [Image #2]). Зразок (с. 80, [Image #4]) — звичайні
  абзаци: абзацний відступ 1,25 см лише на першому рядку, перенесені
  рядки від лівого поля, за шириною.
- Очікувано: §1.1 — абзацний відступ 1,25 см, вирівнювання за шириною;
  §11 — бібліоописи як суцільний текст за зразком.
- Фікс: `build_pdf.py` — для `12-spysok-dzherel.md` нумеровані позиції
  тепер рендеряться як `<p class="ref">` (`text-indent: 12.5mm;
  margin: 0; text-align: justify`), а не як елементи списку з відступом
  блоку. Підтверджено рендером с. 50 — збігається зі зразком.

### [ВИПРАВЛЕНО] [MAJOR] §5 — назва таблиці відірвана від таблиці
- Було: підпис «Таблиця 6.1 – …» на с. 37, сама таблиця — на с. 38
  ([Image #5]). §5 вимагає назву безпосередньо над таблицею, без розриву
  сторінки між ними; між назвою і таблицею порожнього рядка немає.
- Очікувано: §5 — «спершу текст, таблиця — на наступній сторінці»;
  назва і таблиця нерозривні.
- Фікс: `build_pdf.py` — підпис «Таблиця N – …» і наступна таблиця
  загортаються в `<div class="tbl-block">` з `page-break-inside: avoid`
  і нульовим проміжком підпис↔таблиця. Підтверджено рендером: с. 37
  завершується текстом, с. 38 — підпис безпосередньо над таблицею 6.1.

## Перевірено за зразком (без зауважень)
- §5 — підпис `Таблиця 6.1 – …` з абзацного відступу, мала літера, без
  крапки; зміст 12 пт; без діагональних ліній; один порожній рядок до
  назви і після таблиці; посилання «в таблиці 6.1» у тексті до появи.
- §11 — порядок за першою згадкою; ДСТУ-формат із «(дата звернення:
  ДД.ММ.РРРР)»; перед додатками; вирівнювання за шириною як у зразку.
- §1.1 — абзацний відступ 1,25 см єдиний для тексту й бібліоописів;
  без зайвих порожніх рядків (текст→таблиця перенесено коректно).

## Потребує ручної перевірки (формат не несе)
- Точні мм полів/кеглі поза CSS; шрифти Liberation Serif/Mono → TNR/
  Courier New для офіційної здачі; висячі рядки (§1.1); табуляційні
  лідери ЗМІСТ (§2.5). Решта категорій — без змін від попереднього
  аудиту (рисунки 7.1–7.4 українською, ≥10 пт; §4 MAJOR раніше усунено).
