# Grill Presentation Report — thesis/presentation.pptx — 2026-06-04 (rev. 2, після перегенерації)

**Перевірено проти:** `docs/diploma-rules/references/PRESENTATION-RULES.md` + **дослівна звірка** з `thesis/03-vstup.md` і `thesis/11-zagalni-vysnovky.md`.
**Витяг:** `python-pptx`. Деку **перегенеровано** з виправленого `build_pptx.py` після того, як попередній аудит (rev. 1 нижче) виявив 3 BLOCKER через застарілість.

## Підсумок

Перевірено категорій: **9/9**. Порушень: **2** (BLOCKER: **0**, MAJOR: **0**, MINOR: **2**).
**Вердикт: PASS WITH FIXES** — усі три BLOCKER попереднього аудиту (мета, об'єкт, неповний перелік задач) і MAJOR (відсутній експеримент) усунено; лишилися дві необов'язкові стилістичні дрібниці.

## Що виправлено (rev. 1 → rev. 2)

| Було (BLOCKER/MAJOR) | Стало |
|---|---|
| Мета (сл. 2): «скорочення часу возз'єднання…» (стара, R3) | **дослівно з `03-vstup.md`:** «підвищення ефективності первинного зіставлення… що автоматично формує ранжований перелік ймовірних збігів» — звірено байт-у-байт |
| Об'єкт (сл. 3): «процес пошуку та повернення…» (стара, R4) | **дослівно:** «процес автоматизації зіставлення… засобами вебзастосунку» — звірено |
| Задачі (сл. 2): 8 із 9 | **9 із 9**, остання — «провести експериментальне дослідження ефективності… на модельному наборі даних» |
| Експеримент відсутній (MAJOR) | **новий слайд 14** «Експериментальне дослідження ефективності зіставлення» (таблиця: Hit@1=0,82, Hit@3/5=1,00, MRR=0,91, медіана 1, ≤50 мс@50k, ~100×); згадку додано й у висновки |

Корінь: `build_pptx.py` мав регулярки під стару редакцію Вступу (витяг об'єкта повертав `None`). Виправлено: мета/об'єкт/предмет/задачі тепер тягнуться **дослівно** з чинного `03-vstup.md`; додано слайд експерименту та рядок про нього у висновки; оновлено нотатки доповідача під нову рамку.

## Порушення

### [MINOR] Таблиця аналогів без рядка «Найкраще підходить для»
- Правило: PRESENTATION-RULES.md «Правила вмісту» п. 5 (рядки «Ціна» і «Найкраще підходить для»).
- Знайдено (слайд 4): є «Безкоштовність базових функцій» (≈ ціна); рядка «Найкраще підходить для» немає.
- Фікс (необов'язково): не додаю автоматично, щоб не вигадувати зміст, якого немає в роботі (анти-патерн «нові факти»). За бажанням — додати вручну.

### [MINOR] Заголовок слайда 3 — порядок слів
- Знайдено: «Об'єкт та предмет»; рекомендовано «Предмет та об'єкт». Обидва пункти присутні в тілі; відхилення несуттєве.

## Потребує ручної перевірки

- **Шрифт ≥ 18 пт** на всіх слайдах — у коді задано 18 пт для тіл, 24–28 пт для заголовків; візуально підтвердити у PowerPoint.
- **Щільність слайда 2** — мета + 9 задач не вміщувалися в стандартну рамку тіла (4,87″). При першому виправленні задано лише `top`/`height` плейсхолдера — а на успадкованому плейсхолдері python-pptx це матеріалізує `xfrm` із `left=width=0`, через що рамка тексту схлопнулася до нульової ширини й текст рендерився по одній літері в рядок. Остаточно виправлено: задано **всі чотири** поля геометрії — L=0,455″, T=1,55″, **W=12,424″** (повна ширина з макета), H=5,65″ (низ 7,20″ з 7,50″); інтервали між абзацами стиснено, кегль 18 пт збережено, жодну задачу не прибрано. Фінально звірити візуально у PowerPoint.
- **Скриншот «авторизація» (слайд 9)** не має окремого рисунка в роботі (у §7.1 авторизація лише описана) — підтвердити прийнятність або замінити.

## Чисті категорії

- **§Формат** — 16:9 (1,778) ✓; 14 нумерованих слайдів (у межах 12–15) ✓; нумерація вгорі праворуч ✓.
- **§Обов'язкові слайди й порядок** — титульний → мета/задачі → об'єкт/предмет → аналоги → діаграми (use-case, архітектура, БД) → стек → контрольний приклад (5) → **експеримент** → висновки ✓.
- **§Титульний** — тема, виконавець + група (АС-222), керівник ✓.
- **§Аналоги** — таблиця зі стовпцем «Власна розробка» ✓. *(Стовпця немає в Таблиці 1.1 роботи — і це правильно за R5; різні артефакти, різні правила.)*
- **§Діаграми** — 3, кожна на окремому слайді, із заголовком і зображенням ✓.
- **§Контрольний приклад** — 5 скриншотів ключових екранів ✓.
- **§Висновки** — переказ + «Поставлені в роботі задачі виконано повністю, мету роботи досягнуто» + рядок про експеримент ✓.
- **§Дослівна звірка з роботою** — мета, об'єкт, предмет і всі 9 задач **байт-у-байт** збігаються з `03-vstup.md` ✓.
- **§Анти-патерни** — без полотен тексту (мета — одне речення, далі марковані тези/таблиці); діаграми з заголовками; нових фактів немає (експеримент — з розділу 6 роботи) ✓.

---

## rev. 1 (2026-06-04, до перегенерації) — для історії

Вердикт був **FAIL**: 3 BLOCKER (мета сл. 2 ≠ Вступу; об'єкт сл. 3 ≠ Вступу; задачі 8/9) + 1 MAJOR (експеримент відсутній). Причина — деку зібрано до доопрацювання роботи за зауваженнями комісії й не перегенеровано. Усунено в rev. 2 (вище).
