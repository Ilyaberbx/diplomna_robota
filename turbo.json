{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "verify": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {},
    "typecheck": {},
    "lint": {}
  }
}
